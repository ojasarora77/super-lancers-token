/**
 * Downloader for CRS from the web or local.
 */
export declare class NetCrs {
    /**
     * The number of circuit gates.
     */
    readonly numPoints: number;
    private data;
    private g2Data;
    constructor(
    /**
     * The number of circuit gates.
     */
    numPoints: number);
    /**
     * Download the data.
     */
    init(): Promise<void>;
    /**
     * Download the G2 points data.
     */
    downloadG2Data(): Promise<void>;
    /**
     * Verification key data.
     * @returns The verification key.
     */
    getG1Data(): Uint8Array;
    /**
     * G2 points data.
     * @returns The points data.
     */
    getG2Data(): Uint8Array;
}
/**
 * Downloader for CRS from a local file (for Node).
 */
export declare class FileCrs {
    /**
     * The number of circuit gates.
     */
    readonly numPoints: number;
    private path;
    private readonly offsetStart;
    private data;
    private g2Data;
    constructor(
    /**
     * The number of circuit gates.
     */
    numPoints: number, path: string, offsetStart?: boolean);
    /**
     * Read the data file.
     */
    init(): Promise<void>;
    /**
     * Verification key data.
     * @returns The verification key.
     */
    getG1Data(): Uint8Array;
    /**
     * G2 points data.
     * @returns The points data.
     */
    getG2Data(): Uint8Array;
}
/**
 * Generic CRS finder utility class.
 */
export declare class Crs {
    /**
     * The number of circuit gates.
     */
    readonly numPoints: number;
    /**
     * Option to save downloaded SRS on file.
     */
    private readonly saveOnFile;
    private crs;
    private logger;
    /**
     * The path to our SRS object, assuming that we are in the aztec3-packages folder structure.
     */
    private devPath;
    /**
     * The path of our SRS object, if we downloaded on init.
     */
    private localPath;
    constructor(
    /**
     * The number of circuit gates.
     */
    numPoints: number, 
    /**
     * Option to save downloaded SRS on file.
     */
    saveOnFile?: boolean);
    /**
     * Read CRS from our chosen source.
     */
    init(): Promise<void>;
    /**
     * Verification key data.
     * @returns The verification key.
     */
    getG1Data(): Uint8Array;
    /**
     * G2 points data.
     * @returns The points data.
     */
    getG2Data(): Uint8Array;
}
//# sourceMappingURL=index.d.ts.map